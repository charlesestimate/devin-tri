# Model Context Protocol check — 3 September 2026

Endpoint `https://elegant-cormorant-29.convex.site/mcp`, session label "Claude Code review", caller resolved as Karl Ivan Estadola. Protocol version negotiated 2024-11-05. Bearer authentication works: no token and a bad token are both refused with a clear message.

## What works

| Check | Result |
|---|---|
| Authentication | Missing token → 401 "Missing Bearer token". Wrong token → 401 "Invalid or revoked session token". |
| Caller identity | `get_session_info` returns the real person, the label, authorised time and last-call time. No agent identity exists. Correct. |
| Tool listing | 15 tools: 14 reads, 1 write (`create_task`). |
| Reads | All 14 return data or an empty list. Exception reports return an evaluation timestamp and a rule count, as the specification requires for "silence states what was checked". |
| Write | `create_task` created task `HT-MCP test task from Claude Code` on PRJ-2026-0001, assigned to Karl, committed 5 September. `list_tasks` returns it with `requestedByPersonId` and `createdBy` both set to Karl. |
| Scope | Every record returned carries the tenant identifier. Only one tenant is visible. |

## Data seen at the time of the check

Five projects PRJ-2026-0001 to 0005, all in `setup`, created by the browser agent between 04:21 and 04:36 UTC. Twenty-plus persons. Three warehouse locations, no stock lines. No purchase orders, incidents, permits, safety stops or tasks yet, apart from the test task.

## Gaps against section 12 of the master prompt

1. **Read coverage is a fraction of what section 12.2 requires.** The specification asks for list, get and search on every object in section 8 plus about twenty computed queries. Present: projects, persons, tasks, finance summary, safety stops, incidents, permits, inventory summary, open purchase orders, board pack, HR exceptions, exception reports. Absent: parties, sites, opportunities, proposals, contracts, blocks, site reports, toolbox meetings, non-conformance reports, transmittals, goods receipts, stock, transfers, documents, threads and messages, notifications, approvals pending with age, deployments, resource requests, payroll periods, service agreements, work orders, warranty claims, audit log search, hash-chain verification.
2. **Write coverage is one tool.** Section 12.3 requires create and update for every object and the ability to submit an approval decision. Only `create_task` exists. An agent cannot write a notification, raise a non-conformance report, record a decision, or post a message.
3. **No configuration tools.** Section 12.4 requires thresholds, gate rows, roles, constants and statutory tables to be configurable over the protocol, with the seven rules including confirmation before applying. None exist.
4. **Exception rules are thin.** Finance 4 rules, safety 4, HR 3, construction 0, procurement 0. The specification lists far more computed reads; construction and procurement return "0 rules evaluated", which reads as silence rather than a check.
5. **Figures do not cite records.** Section 12.1 requires every returned figure to cite the records it came from. Summaries return bare counts and totals.
6. **`hardBlock6Active` is false on a project with no contract.** On PRJ-2026-0001 the finance summary shows `contractValue: null` and `hardBlock6Active: false`. With no signed contract uploaded, hard block 6 should be in force. Either the flag has the opposite meaning or the evaluation is wrong. Needs a fix or a rename.
7. **Project record carries no site or client reference.** `list_projects` returns opportunity, proposal and site assessment identifiers but no `siteId` and no client party. The specification makes `site_id` required on project. The screen may show the client through the opportunity, but the record itself does not link to the site.
8. **Money visibility and record scope over the protocol are untested.** Tests 159 and 160 need a person with `money_visibility` of `none` and a project outside their scope. Karl sees everything, so this check waits for the second-phase accounts.
9. **Protocol version.** The server negotiates 2024-11-05 rather than the current streamable HTTP revision. Claude Desktop and Claude Code connect fine to either, so this is low priority, but no session identifier header is returned.

## To do in the platform now

- Open Administration → Audit Chain and find the entry for the task created at 04:47:09 UTC. It should name Karl, carry arrival channel `model_context_protocol` and the agent session identifier `n572k0wykrhdpprhrsksca3xks8dpydn`. If any of the three is missing, that is a finding.
- Revoke the "Claude Code review" session from the Agent Sessions tab once done. Then call the endpoint again with the old token: it must be refused.
- Close or leave the `HT-MCP` task; it is harmless.
